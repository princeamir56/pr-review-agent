{
  "name": "pr-review-agent-web-server",
  "version": "0.1.0",
  "private": true,
  "description": "HTTP + SSE API that fronts the pr-review-agent MCP orchestrator for the web dashboard.",
  "main": "dist/web/server/src/index.js",
  "type": "commonjs",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/web/server/src/index.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@octokit/rest": "^21.1.1",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "pr-review-agent-web": "file:..",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
